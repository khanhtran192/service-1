apply plugin: "war"

bootWar {
    mainClass = "com.mycompany.myapp.Service1App"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
